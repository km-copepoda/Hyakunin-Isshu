# Vercel Postgres (Neon backed) connection strings
# Vercel ダッシュボードで Storage → Create Database → Postgres を作成すると
# 自動で .env.local に注入される。手元で動かす場合のみコピーして使う。
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=
